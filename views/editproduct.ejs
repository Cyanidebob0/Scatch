<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Create Product - Scatch</title>
  </head>
  <body class="bg-[#F5F5F5]">
    <div class="min-h-screen flex flex-col items-center justify-center px-4">
      <h1 class="text-4xl font-bold tracking-tighter mb-10">Scatch</h1>

      <div class="w-full max-w-md bg-white rounded-xl shadow-lg p-8">
        <h2 class="text-2xl font-semibold mb-6 text-center">Edit a Product</h2>
        <form
          action="/products/editproduct/<%=product._id%>"
          method="POST"
          enctype="multipart/form-data"
        >
          <label class="block text-sm font-medium mb-1" for="productName"
            >Product Name</label
          >
          <input
            class="w-full h-10 rounded-xl p-2 outline-none border border-gray-300 mb-4"
            type="text"
            name="productName"
            id="productName"
            value="<%=product.name%>"
            placeholder="Enter product name"
          />

          <label class="block text-sm font-medium mb-1" for="productImage"
            >Product Image</label
          >
          <input
            class="w-full h-10 rounded-xl p-2 file:mr-4 file:py-2 file:px-4 file:rounded-xl file:border-0 file:text-sm file:bg-green-100 file:text-green-700 mb-4"
            type="file"
            name="productImage"
            id="productImage"
            accept="image/*"
          />

          <label class="block text-sm font-medium mb-1" for="price"
            >Price</label
          >
          <input
            class="w-full h-10 rounded-xl p-2 outline-none border border-gray-300 mb-4"
            type="number"
            name="price"
            id="price"
            value="<%=product.price%>"
            placeholder="Enter price"
            min="0"
          />

          <label class="block text-sm font-medium mb-1" for="discount"
            >Discount (%)</label
          >
          <input
            class="w-full h-10 rounded-xl p-2 outline-none border border-gray-300 mb-6"
            type="number"
            name="discount"
            id="discount"
            value="<%=product.discount%>"
            placeholder="Enter discount percentage"
            min="0"
            max="100"
          />

          <button
            type="submit"
            class="w-full h-10 rounded-xl p-2 bg-green-400 hover:bg-green-500 transition"
          >
            Save Changes
          </button>
        </form>

        <div class="text-center mt-6">
          <a
            href="/mainpageowner"
            class="inline-block text-sm text-blue-700 hover:underline hover:text-blue-900"
          >
            ← Go Back to Home
          </a>
        </div>
      </div>
    </div>
  </body>
</html>
