<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Scatch - Login</title>
  </head>
  <body>
    <% if (error && error.length > 0) { %>
    <div
      class="absolute top-5 left-1/2 -translate-x-1/2 z-50 bg-red-500 text-white p-2 rounded-xl shadow-lg"
    >
      <span class="inline-block mt-1 mb-1 text-white"><%= error %></span>
    </div>
    <% } %>
    <div class="h-screen w-full flex">
      <div class="h-full w-1/2 bg-[#F5F5F5] relative">
        <div class="h-20 p-6">
          <h1 class="text-4xl font-bold tracking-tighter">Scatch</h1>
          <div
            class="w-[25vw] rounded-xl top-1/2 left-1/2 absolute -translate-x-1/2 -translate-y-1/2"
          >
            <h1 class="text-4xl mb-16">Welcome Back</h1>
            <form action="/users/login" method="POST">
              <h3 class="text-l mt-5">Email</h3>
              <input
                class="w-full h-10 rounded-xl p-2 outline-none border border-gray-300"
                type="email"
                placeholder="Enter your email"
                name="email"
                required
              />
              <h3 class="text-l mt-5">Password</h3>
              <input
                class="w-full h-10 rounded-xl p-2 outline-none border border-gray-300"
                type="password"
                placeholder="Enter your password"
                name="password"
                required
              />
              <div class="flex mt-5 gap-2">
                <input type="checkbox" id="remember" name="remember" />
                <label class="text-sm" for="remember"> Remember me </label>
              </div>
              <button class="w-full h-10 rounded-xl p-2 bg-green-400 mt-10">
                Login
              </button>
            </form>
            <p class="mt-5 text-center">
              Don't have an account?
              <span
                ><a class="hover:text-blue-700 cursor-pointer" href="/"
                  >Sign Up</a
                ></span
              >
            </p>
          </div>
        </div>
      </div>
      <div
        class="h-full w-1/2 bg-[url(images/home-picture.jpg)] bg-cover bg-center -rounded-2xl"
      ></div>
    </div>
  </body>
</html>
